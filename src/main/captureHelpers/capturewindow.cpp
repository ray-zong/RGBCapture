#include "capturewindow.h"

#include <QTimer>
#include <QDebug>

TaskMetadata CaptureWindow::execute(TaskSettings *pTaskSettings)
{
    qDebug() << __FUNCTION__;

    TaskMetadata metadata;
    return metadata;
}

